# LibreToybox — Completed Work

Finished items moved out of `plan.md`, newest first within each section.

## Odd One Out (`odd-one-out/index.html`)

- ~~**Clearer wrong-tap feedback**~~ ✓ (2026-07-16) — a wrong tap was only a small shake + a quiet "hmm," easy to miss. Added the suite's shared "wrong" cue: the tapped tile now also flashes red (`wrongFlash` keyframe — border/background briefly go `#E53935`/`#FFCDD2`, matching Sudoku's `.conflict`, then settle straight back to neutral so nothing persists), the shake is a bit stronger, and there's a short `navigator.vibrate(60)` for a tactile cue on touch devices. Still non-punitive (design principle 3) — no penalty, no advance, the child just tries again. Verified with Playwright (both `.tile.wrong` animations run, border hits `rgb(229,57,53)` mid-flash and returns to the gold border after) and a screenshot.
- ~~**Implement the game**~~ ✓ (2026-07-16) — sixth game shipped per the scoping conversation: a spot-the-difference puzzle where the child taps the item that doesn't belong, discovering the hidden rule (what the *others* share). A self-verifying `generateRound()` draws the majority from one group and exactly one odd from a **disjoint** other group, so there's always a single unambiguous answer. Two data tiers: disjoint super-categories (`GROUPS`) for the easy/medium/hard levels and sibling sub-categories (`SUBGROUPS`, e.g. a land animal among sea animals) for the subtle 🦁 tier where the rule is genuinely hidden. Difficulty is the one setting (🐣 4 / 🐤 6 / 🐥 9 / 🦁 9-subtle tiles); tiles are deliberately uniform-colored so the emoji's category is the only variable. Progression is sets of 5 correct rounds → the shared confetti + wiggle-emoji party, tracked by visual pips. Feedback is humane-first: correct pops + fills a pip + advances; wrong plays the gentle "hmm" and shakes only the tapped tile with no penalty. Reuses the standard header/settings/audio/PWA scaffolding, `gameId`-guarded advance timer, real-`<button>` tiles for keyboard parity, and a sunny yellow/gold identity. Architecture notes in `CLAUDE.md`.

## Child Sudoku (`sudoku-for-minis/index.html`)

- ~~**"Wrong numbers" setting covers wrong AND illegal numbers**~~ ✓ (2026-07-13) — the toggle used to react only to rule conflicts; a legal-but-wrong digit slipped through silently. Since every puzzle has a unique solution, "wrong" is now simply "not the solution digit" (`isWrong()`): reject mode flies both kinds back to the pad, keep mode leaves both red until fixed, and switching back to reject clears them with the usual suction. The hint text says so ("Wrong or clashing numbers…"), and it now sits directly under the "Wrong numbers" label, tight against the toggle, instead of floating as a separate centered line. Verified with Playwright: legal-but-wrong placement flies back / stays red / clears on toggle-back, correct digits still place.

- ~~**Feat: bigger difficulty levels — 1–6 and 1–8**~~ ✓ (2026-07-13) — settings gained a board-size radio (same pattern as Memory/Shape Fit): 🐣 4×4 (digits 1–4, 2×2 boxes, the default) / 🐥 6×6 (1–6, 2×3 boxes) / 🦁 8×8 (1–8, 2×4 boxes). All board geometry lives in a `SIZES` table (`n`, `boxR`×`boxC`, givens target ~50% at every size, per-size number font); the generator, conflict checks, win check and renderer are fully parametrized, and the number pad is built dynamically (digits 5–8 continue the canonical pastel set with darker same-hue text, wrapping 3+3 / 4+4). Generation stays instant (≤3 ms max at 8×8 over 300 runs, all uniquely solvable). **Side fix:** the old static `nth-child` block borders sat on the wrong boundaries (after columns/rows 0 and 2 instead of the middle), so the 4×4 never actually showed its 2×2 boxes — borders are now set per cell from the box geometry. Verified with Playwright: size switching, box-border positions at all three sizes, a full 6×6 solve to the win overlay via real clicks, and placement/conflict at 8×8.
- ~~**Selected cell highlight**~~ ✓ (2026-07-13, UI-coherence pass) — the lime dashed outline became a solid 4 px teal (`--c2`) outline plus a tinted `#E6FAF8` fill, distinct and on-palette.
- ~~**Win screen should keep puzzle visible**~~ ✓ (2026-07-13, UI-coherence pass) — copied Memory's pattern: translucent `rgba(255,255,255,0.55)` win overlay with bottom-anchored content; settings overlay keeps the opaque white.
- ~~**Visible feedback when Reject mode clears illegal cells**~~ ✓ (AUDIT A11) — `removeIllegalCells()` now plays the swoosh sound and the same suction animation a manual clear uses, per cell, instead of silently zeroing the board.
- ~~**Keyboard access for grid cells**~~ ✓ (AUDIT A12) — non-given cells are tabbable (`tabIndex = 0`), a shared `activateCell(r, c)` drives both pointer taps and Enter/Space, and there's a `:focus-visible` outline distinct from the tap-selected one. `design_principles.txt` §8 wording fixed to say "keyboard, mouse, and touch."
- ~~**Dead code cleanup**~~ ✓ (AUDIT A16) — removed `tryClear()`, `bonkCell()`/`audioBonk()` (plus the now-orphaned `.cell.bonk`/`@keyframes bonk` they triggered), the `--eraser` CSS variable, `@keyframes shake`, `.hidden` class, and the `.emoji-row span:nth-child(4)` rule.
- ~~**Inconsistent interaction**~~ ✓ — the active number now resets after every placement; each piece requires picking a number again (no latent selection).

## Exquisite Corpse (`exquisite-corpse/index.html`)

- ~~**Remove the top icon/header to make more room for the canvas**~~ ✓ (2026-07-15) — the `<header>` (logo + New Game/Settings) is gone; New Game and Settings now live in the `#phase-banner` row that used to just show the section emoji, reclaiming a full row of vertical space for the canvas. The current section is no longer announced visually via emoji — it's conveyed by a new grey `#next-peek-overlay` band pinned to the *bottom* of `#canvas-wrap` (mirroring the existing top `#peek-overlay`), marking the strip that will carry over as the next player's peek guide; it hides automatically on the final section, since there's no next player. `#status` still announces the section by name for screen readers.
- ~~**Bug: winning reveal covered by the last drawing section**~~ ✓ (reported & fixed 2026-07-12) — after the final ✓, the bottom part of the painting remained on screen and covered the final revealed image: `#reveal-canvas` was transparent outside the letterboxed artwork, so the main `#drawing-canvas` — still translated to the legs section — showed through around the reveal. `reveal()` now fills the reveal canvas with opaque paper white before `drawImage()`, fully covering the drawing canvas. `sw.js` cache bumped to `fold-and-pass-v2` per the release rule.
- ~~**Enlarge toolbar controls to 64×64 px**~~ ✓ (2026-07-13, AUDIT A6) — swatches and brush/undo/clear buttons are now 64×64; `#color-row`/`#tool-row` gained `flex-wrap: wrap` so six 64 px swatches (~424 px) wrap to two rows on narrow phones instead of overflowing (the tall canvas is `flex: 1` and absorbs the extra row).
- ~~**Bug: in-progress stroke lost on resize**~~ ✓ (2026-07-13, AUDIT A18) — `redrawCanvas()` now draws `currentStroke` after the committed strokes, so a stroke in progress survives a rotation-triggered redraw.
- ~~**Bug: final screen is "squished"**~~ ✓ (AUDIT A7) — `reveal()` now renders the full artwork onto an offscreen buffer at its true (undistorted) aspect ratio, then letterbox-scales that buffer into the wrap-sized overlay instead of stretching x/y independently. `downloadPainting()` exports the undistorted buffer directly (`artworkCanvas`), so the saved PNG is no longer squished either.
- ~~**Canvas-mode change destroys the current drawing**~~ ✓ (AUDIT A11) — `setCanvasMode()` no longer calls `newGame()`; it resizes the canvas for the new aspect and redraws existing strokes (normalized coordinates already survive resize, the same mechanism the window-resize handler uses), so switching Tall/Wide preserves the drawing in progress.
- ~~**Full section hiding between players**~~ ✓ — canvas slides vertically; completed sections off-screen, `PEEK_PX: 40` strip visible as connection guide.
- ~~**Tall/Wide canvas setting**~~ ✓ — Settings ⚙️ radio: Tall (`flex:1`, portrait) for phones; Wide (`aspect-ratio: 4/3`) for tablets. Changing mode starts a new game.
- ~~**Download finished painting**~~ ✓ — 💾 button on win screen saves `fold-and-pass.png` via `canvas.toDataURL`.
- ~~**Rename "Combination Man"**~~ ✓ — renamed to **Fold and Pass** (2026-07-12): title, manifest `name`/`short_name`, download filename, and the `sw.js` cache name (bumped to `fold-and-pass-v1` per the release rule in CLAUDE.md) all updated.
- ~~**Win screen redesign**~~ ✓ — No confetti. Painting stays visible (reveal-canvas overlay, never resizes main canvas). Tools hidden with `visibility:hidden` (not `display:none`) so canvas position never jumps. Download + Replay replace the toolbar area.
- ~~**Section emojis**~~ ✓ — 🙂 head / 👕 body / 👖 legs. No player number labels.
- ~~**Automatic section transitions**~~ ✓ — No handoff pause screen. Slides to next section after 460 ms animation.

## All games

- ~~**Back button on every game**~~ ✓ (2026-07-15) — a 🏠 link back to the hub (`../index.html`) now sits in the same spot in every game: the first, flush-left element of the header (or `#phase-banner` in Fold and Pass, which has no `<header>`/logo). Sudoku, Memory and Shape Fit group it with their logo inside a new `.header-left` wrapper so `header { justify-content: space-between }` still resolves to exactly two flex children (`.header-left` left, `.top-btns` — New Game + Settings — right); Fold and Pass's `#phase-banner` puts the Back link alone on the left and reintroduces a `.top-btns` cluster on the right for New Game + Settings. Implemented as a plain `<a class="top-btn" href="../index.html">`, not a JS handler, so it works with no script and gets keyboard Enter activation for free. Verified with Playwright across all four games: identical top-left position and size, correct navigation to the hub on click and on keyboard Enter, and `npm run ci` (lint + link check) clean.
- ~~**Tappable celebration emojis**~~ ✓ (2026-07-13) — the win-screen emojis in Sudoku, Memory and Shape Fit are now real `<button>`s (keyboard parity, principle 8): a tap/Enter plays a 500 ms wiggle (rotate + scale keyframes, retriggerable mid-animation) and a random pentatonic chirp (`audioEmoji()`). Fold and Pass has no emoji row — its celebration is the artwork reveal itself, so nothing to do there. Side fix: Sudoku's emojis were plain inline `<span>`s, so its pop entrance transform never actually applied; buttons are inline-block, so the entrance animation now works there too.
- ~~**UI / color coherence pass**~~ ✓ (2026-07-13) — one color system across the toybox (now design principle 11): Memory's saturated card-back gradient replaced with the shared purple pastel + darker edge (`#C3B4F7`/`#9480DC`); Sudoku's stray hot pink (`#FF69B4`) unified to its coral `--c1`; Fold and Pass's vivid `#7C4DFF` accent and Material greens softened to `#9480DC`/`#66BB6A`, and its dark settings overlay switched to the translucent white every other game uses; confetti everywhere draws from the shared palette; hub cards shrunk to a 2×2 grid with hover/focus states. Cache names bumped: `memory-v2`, `child-sudoku-v3`, `fold-and-pass-v3`.
- ~~**Service-worker update strategy**~~ ✓ (2026-07-13, AUDIT B1) — all four `sw.js` workers switched from cache-first to **stale-while-revalidate**: cached version served instantly (offline-first load unchanged), cache refreshed in the background, next visit gets the new version automatically. Routine releases no longer require a cache-name bump; the bump remains only as an emergency "everyone must get this now" lever (CLAUDE.md updated). Verified end-to-end with a persistent browser profile: change deployed → next load stale, following load current, offline load still works. No cache names were bumped — browsers byte-diff `sw.js` itself on navigation, so deployed players pick up the new worker automatically.
- ~~**Favicon**~~ ✓ (AUDIT B3) — all three pages (hub + both games) declare `<link rel="icon">` using an inline 🧸 emoji SVG data URI, matching the teddy bear already used as the hub's logo.
- ~~**Use or remove the `aria-live` status regions**~~ ✓ (AUDIT A17) — both games now announce meaningful state: Sudoku announces cell selection, placements, conflicts, and the win; Fold and Pass announces the current section (head/body/legs) and the final reveal.

## Emoji Paint (`emoji-paint/index.html`)

- ~~**Removal redesign: long-press-to-lift-and-drag + tap-to-arm trash, replacing the eraser**~~ ✓ (2026-07-16) — further feedback on the removal mechanism: the eraser tool and its Sudoku-matched circle-fill hold-to-delete were both replaced with a two-path model. (1) Long-press an existing stamp to lift it (soft accent-colored "picking up" halo during the hold, then the stamp rides enlarged with a drop shadow) — drop it on the trash button (🗑️, replacing the sponge) to delete it, or drop it anywhere else on the canvas to reposition it there. This is a genuinely new capability: stamps can now be moved after placement, not just deleted. (2) Tap the trash button to arm a one-shot delete mode (reddish highlight); the next tap on any stamp removes it and the mode disarms itself immediately, so it can never stay silently armed — the standing requirement from earlier feedback. A tap on empty canvas while armed does nothing (stays armed) rather than drawing. Both paths converge on the same `deleteStamp()`/`animateRemoval()` (shrink-fade + swoosh + vibrate). One pointer-gesture recognizer (`onPointerDown`/`onPointerMove`/`onPointerUp` on `lifting`) handles tap-vs-lift disambiguation for both paths without conflict — holding never cancels on movement, so a stamp can drift before the lift threshold completes without losing the gesture. Verified with Playwright: toolbar shows trash not eraser; a quick tap on an existing stamp is a no-op when unarmed; tap-to-arm deletes and auto-disarms; empty-canvas taps while armed are no-ops that stay armed; holding past the lift threshold marks a stamp "lifted"; dragging and releasing elsewhere repositions it to the drop point; the trash button highlights while a lifted stamp hovers it; dropping on the trash deletes it; and picking a new stamp from the picker disarms trash mode.
- ~~**UI consistency fixes: circle-fill removal to match Sudoku, drop the trash button, Undo → Eraser**~~ ✓ (2026-07-16) — feedback on the removal feature just above. (1) The hold-to-remove feedback was a hand-built shake + pulsing ring that didn't match Sudoku's actual long-press-to-clear pattern; replaced with a faithful canvas port of Sudoku's real mechanism — a reddish pie (`rgba(255, 107, 107, 0.85)`) that fills in like a clock face over exactly 500 ms (Sudoku's threshold, down from this feature's original 550 ms), plus the same `navigator.vibrate(40)` on completion. (2) Removed the toolbar Clear/trash button entirely — New Painting (header 🔄) already resets the whole board, so it was redundant; `clearCanvas()` deleted as dead code. (3) Undo was replaced with an Eraser tool (🧽): selecting it highlights the button with a reddish border/background (distinct from the game's normal pink accent) and switches canvas touches to erase-on-contact — instant, no hold needed, since choosing the tool is itself the deliberate gate — including drag-to-erase across multiple stamps in one gesture. Picking a new stamp automatically exits eraser mode. A new `erasingStamps` set keeps a stamp mid-removal-animation from being hit-tested again by a lingering finger. Also added two related notes to `plan.md`: canvas games (Exquisite Corpse, Emoji Paint) don't make good use of tablet screen space (capped at `max-width: 480px`), tracked as a future scoping item. Verified with Playwright: trash/undo buttons gone, a mid-hold progress fraction confirming the fill animation runs, full long-press removal, eraser toggle + reddish highlight, instant tap-erase, multi-stamp drag-erase, erasing doing nothing on empty canvas, and eraser mode auto-exiting on stamp selection.
- ~~**Picker polish: single-row Recent, text-free category dividers, long-press to remove a placed stamp**~~ ✓ (2026-07-16) — three follow-ups to the jump-chip/Recent-row feature above. (1) The Recent row now stays a single horizontally-scrolling line (`.recent-grid { flex-wrap: nowrap; overflow-x: auto }`) instead of wrapping into a second row. (2) Category sections dropped their text `<h3>` labels in favor of a discreet `.picker-divider` line — the nav chips already carry each category's identity, so the heading was redundant text (principle 1). (3) Long-pressing an already-placed stamp now removes it: `findStampAt()` hit-tests the pointerdown against the `stamps` array (topmost first), and a hold past 550 ms deletes it with a canvas-drawn port of Sudoku's removal feedback — a reddish pulse + shake that intensify with hold progress, then a shrink-and-fade plus the existing swoosh sound — since stamps are drawn on `<canvas>` rather than DOM nodes and can't carry Sudoku's `.conflict`/`.cleared` CSS animations directly. Releasing early or dragging away cancels the hold with no effect. Verified with Playwright: dividers with zero remaining text labels, the Recent row's items staying on one row (monotonically increasing `left`, single shared `top`) while overflowing into horizontal scroll, a quick tap surviving, a completed long-press removing the stamp, and drifting away mid-hold canceling the removal.
- ~~**Feat: easier to find a stamp in the ~320-emoji picker**~~ ✓ (2026-07-16) — sifting through every emoji cold to find the right one was overwhelming. Added a horizontally-scrolling row of jump chips (one per category + a 🕒 Recent chip) above the grid: tapping one scrolls straight to that section, and an `IntersectionObserver`-driven scroll-spy keeps the matching chip highlighted as the child scrolls by hand. Also added a Recent row (last 8 picks, newest first, deduped) that stays hidden until the first stamp is chosen, so a kid reusing the same handful of favorites never has to scroll past the categories again. Side fix caught by the Playwright regression test: the picker's `scrollTop` reset on open was a silent no-op because it ran before the overlay's `display:none` was cleared — reordered to set it after `.show` is added.
- ~~**Implement the game**~~ ✓ (2026-07-15) — free-play painting canvas shipped per the scoping conversation: shares Exquisite Corpse's normalized-coordinate canvas, Tall/Wide setting, PWA boilerplate and audio helpers, but strips out sections/passing/reveal entirely (single always-visible canvas, no win state). Color and brush-size controls are replaced by an emoji stamp picker — `#stamp-btn` opens a scrollable ~320-emoji grid (`EMOJI_CATEGORIES`, 8 categories) the child can sift through; tapping one selects it as the active stamp. Placement is tap-to-place: a pointer-down picks up a stamp that follows the finger translucently, and pointer-up drops it at the final spot (a quick tap and a drag-then-release both go through the same commit path). Stamps render via `ctx.fillText` with an emoji-font fallback stack, undo/clear/New Painting all operate on a flat `stamps` array, and `downloadPainting()` saves the canvas directly since there's no reveal step to letterbox. Architecture notes in `CLAUDE.md`.

## Repository / Infrastructure

- ~~**Lightweight CI**~~ ✓ (2026-07-15, AUDIT recommendation) — `.github/workflows/ci.yml` runs `html-validate` over `index.html` + `*/index.html` (config in `.htmlvalidate.json`, tuned to this codebase's real conventions: self-closing void elements, no `no-implicit-button-type`/`prefer-native-element` since there are no `<form>`s and the custom-styled selection buttons are deliberate) plus a new dependency-free `scripts/check-links.js` that verifies every local `href`/`src` resolves to a real file on disk (external/`data:`/fragment links are skipped on purpose — no network dependency in CI). Added `.gitignore` (`node_modules/` etc.) ahead of the tooling, plus `package.json`/`package-lock.json` scoped to this dev tooling only — the games ship exactly as before, with zero runtime dependencies. Side fixes surfaced by the linter: the four games' static `<link rel="manifest">` tags (missing `href` before JS sets the real blob URL) got a harmless inline placeholder (`data:application/json,%7B%7D`); five `aria-label` divs (`.logo` in Memory/Shape Fit/Sudoku, `#board`/`#tray` in Shape Fit) that lacked a role got `role="img"`/`role="group"` so the label is actually exposed to assistive tech. Verified: `npm run ci` passes clean, and a broken-link smoke test confirms the checker actually fails on a real dangling link.
- ~~**Feat: free CI/CD hosting**~~ ✓ — `.github/workflows/deploy-pages.yml` deploys the repo to **GitHub Pages** on every push to `main`, live at <https://khaal.github.io/LibreToybox/>. Zero external accounts, zero build step — the static tree is served as-is via `actions/upload-pages-artifact` + `actions/deploy-pages`. Repo was made public and Pages Source set to "GitHub Actions" on 2026-07-12; first deploy verified live.

## Memory (`memory/index.html`)

- ~~**Win screen should show the full board face-up**~~ ✓ (2026-07-15) — matched pairs used to fly off during play, leaving the celebration over an empty board. `celebrate()` now calls a new `revealBoard()` that brings every `.gone` card back face-up, staggered like the initial deal, right before the win overlay shows. Side fix: the reveal's pop bounce (`.matched-pop`) was animating `.card-inner`, the same element that owns the `rotateY` flip transform — a `transform`-animation on that element overrides the flip for its duration, so revealed cards flashed back to their unflipped "❓" backs. Moved the bounce to animate the outer `.card` instead (matching the existing `.deal` pattern), which also incidentally fixes the same glitch in the brief in-game match-found bounce. Verified with Playwright: a full 4×4 game solved via real clicks ends with all 16 cards visible, face-up, and un-flipped-back, with no regression to normal match/mismatch mid-game behavior.
- ~~**Brighter card backs**~~ ✓ (2026-07-13) — card backs moved from the purple pastel to the canonical palette's brighter sky blue (`#8ED3F4`/`#54A6D2`), and the game's accent (theme-color, logo, manifest icon) followed into the blue family so the whole game sits in its ice-blue background hue (design principle 11). Cache bumped to `memory-v3`.
- ~~**Implement the game**~~ ✓ (2026-07-12) — pairs-matching game shipped per the Future Games scoping: face-down emoji cards, 3D flip, match chime / gentle mismatch flip-back (never blocks play — a third tap resolves the pair immediately), matched pairs fly off the board leaving their grid slot empty, 🐣 4×4 / 🦁 6×4 size setting with visible re-deal, translucent win overlay, no timer or move counter. Architecture notes in `CLAUDE.md`.

## Shape Fit (`shape-fit/index.html`)

- ~~**Bug: miscolored background on overscroll**~~ ✓ (reported & fixed 2026-07-13) — with no background on `html`, the `body` gradient propagated to the canvas and **tiled** (`background-repeat` defaults to `repeat`), so rubber-band overscroll revealed the wrap-around of the next tile: the wrong end of the gradient. Fixed by giving `html` its own solid `var(--bg1)` background (the gradient's top color), which stops the propagation — the canvas beyond the page is now a solid same-family pastel. The same latent bug existed on **every** page, so the fix went into all four games and the hub.
- ~~**5×5 is the default board**~~ ✓ (2026-07-13) — a fresh game now opens on the 🐤 5×5 (`mini`) board instead of the 🐥 6×6; the middle of the ramp is the right starting point for the age band.
- ~~**Drag "carry" felt off**~~ ✓ (reported & fixed 2026-07-13) — four compounding causes: (1) the grab point was ignored — on touch the piece teleported to a canonical pose (horizontally centered, bottom edge 56 px above the finger), (2) tray pieces doubled in size in the same frame (tray scale 0.5 → 1), (3) the finger→piece offset grew with piece height (a 3-tall piece floated up to ~200 px above the fingertip, landing cells above where the child pointed), and (4) snapping was a single `Math.round` candidate, so a near-miss beside an occupied region failed outright. Now the piece is carried by its **grabbed point** (normalized, so the tray scale maps to the same spot), riding `liftPx` above the finger (no lift for mouse); pickup eases over 130 ms (`.lifting`) instead of teleporting; and the snap target is the nearest *fitting* cell within `snapRange` 0.8 cells. Verified with Playwright: grab-point tracking to sub-pixel, forgiving-snap unit probes, a full solve via sloppy (±0.3-cell) mouse drags, and a CDP touch drag confirming the 56 px lift and capture.
- ~~**Hardest board eased: 8×8 → 7×7**~~ ✓ (reported & fixed 2026-07-13) — even with the new 5×5 smoothing the ramp, the 🦁 8×8 (~14 pieces) was still too hard; replaced with a 7×7 (pieces 3–6, ~10 pieces), keeping the ramp ~4 / ~6 / ~8 / ~10 pieces and giving the hard board bigger cells. Generator invariants re-verified for n=7 (4 000 boards) plus a Playwright solve-to-win run.
- ~~**5×5 intermediate board**~~ ✓ (reported & fixed 2026-07-13) — the jump from 🐣 4×4 (~4 pieces) to 🐥 6×6 (~8 pieces) was too steep and the hardest level too hard for the target age; added a 🐤 5×5 (`mini`, pieces 3–5, ~6 pieces) between them, so sizes are now 🐣 4×4 / 🐤 5×5 / 🐥 6×6 / 🦁 8×8. The size row wraps to a 2×2 grid (`max-width: 162px`) so four 72 px buttons fit narrow phones. Generator invariants re-verified for n=5 (4 000 boards: full tiling, connected pieces ≥ 3 cells, no adjacent same colors) plus a Playwright solve-to-win run.
- ~~**Bug: dragged piece lands off-screen after a size switch / re-deal**~~ ✓ (reported & fixed 2026-07-12) — the tray-slot deal animation (`animation-fill-mode: both`) left a permanent `transform: scale(1)` on every slot, and an ancestor with any transform becomes the containing block for `position: fixed`, so the dragged piece's viewport coordinates were interpreted relative to its tiny tray slot. Only animated deals were affected, which is why the initial (non-animated) 6×6 board worked and every size switch or 🔄 broke. Fixed by removing the `deal` class on `animationend` (plus on `pointerdown`, for pieces grabbed mid-pop); e2e test now drags for real after animated re-deals. `sw.js` cache bumped to `shape-fit-v3`.
- ~~**4×4 board + piece outlines**~~ ✓ (reported & fixed 2026-07-12) — added a 🐣 4×4 board (~4 pieces) for the youngest players (sizes now 🐣 4×4 / 🐥 6×6 / 🦁 8×8), and gave every piece a darker perimeter outline: blocks now merge into one solid shape inside a piece, with the outline/gap/bevel drawn only around the polyomino's outer edge. Fixes same-colored pieces blending together when placed side by side — free placement means the generator's no-adjacent-colors guarantee can't cover what the player builds.
- ~~**Implement the game**~~ ✓ (2026-07-12) — no-timer "reverse Tetris" shipped per the scoping: randomized region-growing cuts the board into 3–6-cell polyomino pieces (solvable by construction; adjacent pieces never share a color), drag with `setPointerCapture` + lift-above-finger + ghost snap preview, free placement with tap-to-lift undo, keyboard parity (Enter/arrows/Escape), translucent win overlay + confetti, 🐣 6×6 / 🦁 8×8 size setting with visible re-deal. **The big board became 8×8 instead of the scoped 9×9** — bigger cells (~45 px vs 40 px) and ~14 rather than ~18 pieces suit the age band better. Verified with a 4 000-board generator invariant test and a Playwright end-to-end run (drag, invalid drop, lift-off, keyboard, solve-to-win, settings). Architecture notes in `CLAUDE.md`.
