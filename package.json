{
  "name": "libretoybox",
  "private": true,
  "description": "Self-contained HTML5 games for kids aged 6-8. Dev-only CI tooling; the games themselves ship with zero dependencies.",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KhaaL/LibreToybox.git"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "lint:html": "html-validate index.html */index.html",
    "check:links": "node scripts/check-links.js index.html */index.html",
    "check:js": "node scripts/check-js.js index.html */index.html",
    "check:design": "node scripts/check-design.js */index.html",
    "ci": "npm run lint:html && npm run check:links && npm run check:js && npm run check:design"
  },
  "devDependencies": {
    "html-validate": "^11.5.6"
  }
}
